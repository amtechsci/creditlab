# Copy to /etc/nginx/conf.d/00-creditlab-limit-req.conf
# (must live in http{} — conf.d is already included from nginx.conf)

# OTP login/register: 3 POSTs per minute per IP
limit_req_zone $binary_remote_addr zone=creditlab_otp:10m rate=3r/m;
